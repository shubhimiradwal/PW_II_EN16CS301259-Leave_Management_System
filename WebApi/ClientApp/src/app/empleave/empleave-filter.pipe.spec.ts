import { EmpleaveFilterPipe } from './empleave-filter.pipe';

describe('EmpleaveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpleaveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
